fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pillow>=10.0
joblib>=1.3
# Keep this pinned to the version used when `models/hand_static.joblib` was created
# to avoid InconsistentVersionWarning / potential incompatibilities.
scikit-learn==1.7.2
python-multipart>=0.0.9
